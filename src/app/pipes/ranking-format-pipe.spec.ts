import { RankingFormatPipe } from './ranking-format-pipe';

describe('RankingFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new RankingFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
